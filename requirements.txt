pandas>=1.5.0
numpy>=1.23.0
scikit-learn>=1.2.0
matplotlib>=3.6.0
seaborn>=0.12.0
openpyxl>=3.1.0
python-dateutil>=2.8.2

# Optional — install separately if available
xgboost>=1.7.0          # used in model.py; falls back to GradientBoostingClassifier
shap>=0.42.0            # used for SHAP beeswarm; falls back to permutation importance
